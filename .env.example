# Copy to .env (git-ignored) and fill in. Used by pipeline/run.py and the demo's live mode.
# The LLM API key (Google Gemini). The same variable is set on Vercel for the deployed demo.
AI_API_KEY=

# Optional: override the model (default: gemini-2.5-pro)
# RVX_MODEL=gemini-2.5-pro
